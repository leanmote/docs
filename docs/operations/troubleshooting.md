# Troubleshooting

- `404` on route: verify path normalization and stage prefix handling.
- `405` on metrics route: confirm method allowance (GET vs GET/POST endpoints).
- Empty payloads: verify required filters and source data availability.

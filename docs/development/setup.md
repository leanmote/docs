# Development Setup

1. Install Python dependencies from `requirements.txt`.
2. Configure environment variables and service credentials.
3. Run local docs with `mkdocs serve` from the `docs` directory.
